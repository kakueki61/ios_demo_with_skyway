# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ios-demo-with-skyway' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  swift_version = '2.3'

  # Pods for ios-demo-with-skyway
  pod 'SkyWay-iOS-SDK', '0.2.2'

  target 'ios-demo-with-skywayTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ios-demo-with-skywayUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
